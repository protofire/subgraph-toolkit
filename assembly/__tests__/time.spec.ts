import {datetime} from "../../lib/date"

const timestamp: u32 = 1647518400

describe("timestamp module convertion.", () => {
  it("truncateDate", () => {
    log<usize>(datetime)
  })

  // it("truncateMinutes", () => {

  // })

  // it("truncateDate", () => {

  // })

  // it("truncateDate", () => {

  // })
})